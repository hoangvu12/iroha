node_modules/
ui/dist/
ui/node_modules/.tmp/

# Local deployment state. `DATABASE_URL=file:./data/iroha.db` writes here.
data/
*.db
*.db-shm
*.db-wal

# Runtime logs. `bun run dev` rewrites these on every start.
*.log

# Ad-hoc request bodies used when probing an upstream by hand.
/longctx.json
/needle.json
/navy_needle.json
/t.json

# Real secrets. `.env.example` is the tracked template.
.env
.env.local
