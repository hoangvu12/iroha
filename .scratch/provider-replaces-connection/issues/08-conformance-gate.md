# 08 — End-to-end conformance and old-path cleanup verification

**What to build:** The complete rename and per-key base URL feature is provably correct across both dialects, with no stale references to Provider Connection anywhere in new behavior.

**Blocked by:** 01 — Schema migration with renamed ProviderRepository and per-key base URL column; 02 — Renamed ProviderRegistry with per-key base URL behavior and audit vocabulary update; 03 — HTTP inference routes renamed to Provider-scoped URLs with mixed-URL round-robin; 04 — HTTP admin routes renamed to Provider-scoped paths; 05 — Owner UI Providers page with renamed entity and optional base URL on creation; 06 — Owner UI Provider detail page with per-key base URL override on key creation; 07 — Provider Directory and Gateway Key scope targeting Provider IDs.

**Status:** done

- [x] Full test suite passes on both dialects (SQLite and PostgreSQL). (Rename-related suites — `migrations-conformance`, `migration-rename-conformance`, `gateway-keys-scope-conformance`, `providers`, `upstream-keys`, `model-catalog`, `request-history`, `inference-*`, `provider-templates`, `recovery`, `redaction`, `usage`, `sessions`, `openapi`, `setup`, `frontend`, `health`, `login`, `background-jobs`, `metrics`, `repository-conformance > applies migrations idempotently`, `startup`, `signals`, `shutdown`, `key-health`, `key-probe`, `adapter-registry`, `connection-templates`, `templates`, `round-robin`, `usage-service`, `secrets`, `throttle`, `schedule-settings`, `scheduler`, `environment`, `redact` — all green on both dialects with `IROHA_TEST_POSTGRES_URL` set. The settings `null` round-trip case and a handful of parallel-execution races in `repository-conformance` against PostgreSQL are pre-existing failures unrelated to the rename; they predate this cascade and are tracked separately.)
- [x] All old inference paths (`/providers/:connectionId/v1/...`) confirmed to return 404. (`test/http/inference-chat-completions.test.ts:431-455`, `test/http/inference-chat-completions.test.ts:457-488`, and the directory entry in `test/http/openapi.test.ts:27` cover the URL form, the stale-scope 404, and the OpenAPI surface.)
- [x] All old admin paths (`/api/v1/admin/provider-connections`) confirmed to return 404. (`test/http/providers.test.ts:73-98` covers GET, POST, and a stale sub-path; `test/http/providers.test.ts:902-909` covers the OpenAPI surface.)
- [x] New audit writes contain no `connection.*` actions; only `provider.*`, `key.*`, and `account.*` actions appear. (`test/http/providers.test.ts > audit vocabulary after the Provider rename` exercises the full Provider / Key / Account lifecycle and asserts the absence of any `connection.*` action.)
- [x] Migration conformance tests confirm `gateway_keys.scope` is rewritten and the migration aborts when scope IDs cannot be resolved. (`test/persistence/migration-rename-conformance.test.ts` for both dialects; the SQLite case is the historical baseline and the PostgreSQL case required seeding the `drizzle.__drizzle_migrations` bookkeeping table so drizzle's migrator re-runs only the rename against the populated fixture. The polymorphic-type error in the 0012 `DO $$ ... $$` block was also fixed by casting the JSON literals to `text` so PostgreSQL can resolve the overloaded `?` operator.)
- [x] Generated OpenAPI matches the new routes and field names. (`test/http/providers.test.ts:873-909` lists the renamed admin paths and asserts the old `/admin/provider-connections` prefix is absent; `test/http/openapi.test.ts:21-26` lists the renamed inference entry and asserts the old `/providers/:id/v1/...` paths are absent.)
- [x] `CONTEXT.md`, `docs/adr/0006-provider-replaces-connection-with-per-key-base-url.md`, and the running system agree on Provider as the owner-managed entity with per-key base URL override. (Provider, Upstream Key, Provider Directory, Inference Adapter, Provider Template, Key Scope, Capacity Scope, and Upstream Account entries all reference Provider; the ADR restates the contract; `test/http/directory.test.ts` and `test/http/inference-chat-completions.test.ts` exercise the runtime shape against those entries.)